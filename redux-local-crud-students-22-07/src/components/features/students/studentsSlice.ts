// createSlice: RTK function that builds a reducer + action creators + action types from one config object
// PayloadAction<T>: TypeScript type describing an action's shape, where T is the type of its payload
import { createSlice,PayloadAction } from '@reduxjs/toolkit';